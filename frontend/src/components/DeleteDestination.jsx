const DeleteDestination = ({ rowObject, backendURL, refreshDestination }) => {

    return (
        <td>
            <form>
                <button type='submit'>
                    Delete
                </button>
            </form>
        </td>

    );
};

export default DeleteDestination;
