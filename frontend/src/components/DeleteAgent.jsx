const DeleteAgent = ({ rowObject, backendURL, refreshAgent }) => {

    return (
        <td>
            <form>
                <button type='submit'>
                    Delete
                </button>
            </form>
        </td>
    );
};

export default DeleteAgent;