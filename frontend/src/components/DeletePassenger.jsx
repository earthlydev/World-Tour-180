const DeletePassenger = ({ rowObject, backendURL, refreshPassenger }) => {

    return (
        <td>
            <form>
                <button type='submit'>
                    Delete
                </button>
            </form>
        </td>
    );
};

export default DeletePassenger;