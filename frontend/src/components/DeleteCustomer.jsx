const DeleteCustomer = ({ rowObject, backendURL, refreshCustomer }) => {

    return (
        <td>
            <form>
                <button type='submit'>
                    Delete
                </button>
            </form>
        </td>
    );
};

export default DeleteCustomer;