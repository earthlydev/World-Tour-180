const DeleteItinerary = ({ rowObject, backendURL, refreshItinerary }) => {

    return (
        <td>
            <form>
                <button type='submit'>
                    Delete
                </button>
            </form>
        </td>
    );
};

export default DeleteItinerary;